{% trans_default_domain 'FOSUserBundle' %}

{% form_theme form '@WHBackendTemplate/Twig/bootstrap_3_layout.html.twig' %}

{{ form_start(form, { 'action': path('bk_wh_user_resetting_reset', {'token': token}), 'attr': { 'class': 'fos_user_resetting_reset' } }) }}
{{ form_widget(form) }}

<div class="submit">
    <input type="submit" class="btn btn-primary" value="{{ 'resetting.reset.submit'|trans }}"/>
</div>
{{ form_end(form) }}